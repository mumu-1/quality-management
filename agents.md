# agents.md — Agent 工作规则（本仓库协作须知）

> 本文件约束在此仓库中工作的所有 AI Agent 与开发流程。请严格遵守。

## 注意事项

1. **每次改动完成后，都必须创建一个对应的 commit**，以便后续追踪和回滚。
   - 改动 = 代码、测试、文档、演示数据、配置等任何仓库内变更。
   - 一个功能/修复/文档任务 = 一个清晰的 commit（消息写清做了什么、为什么）。
   - 禁止把多次无关改动混在一个 commit 里，也禁止"做完不留痕"。

2. **每次改动后，都必须编写或更新相关测试**，并在给用户交付前，确保所有测试和验证全部通过。
   - 新增功能 → 补对应 e2e/接口测试（本项目模式：`scripts/e2e_step*.py` 真实验证）。
   - 修改行为 → 更新受影响的既有测试断言。
   - 交付前必须跑：本轮新测试 + 相关回归测试，全部绿灯才算完成。
   - 验证结果以真实运行输出为准，不得口头宣称成功。
