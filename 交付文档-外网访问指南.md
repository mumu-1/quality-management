# 生产质量管理系统 · 外网访问指南（远程访问）

> 目标：在公司以外（家里、客户处、出差路上）用电脑或手机打开这套系统。
> 本文按**推荐顺序**给几条做法，任意选一条即可；做完都有"怎么验证"。

---

## 0. 先做这 3 件事（暴露到公网前必须做）

| # | 做什么 | 命令 / 位置 |
|---|---|---|
| 1 | **改掉演示弱口令**（现在是 123456，公网上一分钟就会被试出来） | `python scripts\set_password.py harden`（全部账号随机强密码并导出密码表）<br>或单独改：`python scripts\set_password.py set admin 你的新密码` |
| 2 | **体检**：逐项检查能不能上公网 | `python scripts\preflight_public.py` |
| 3 | **确认每日备份在跑** | 见《运维手册》第三节；确认 `scripts\daily_backup.bat` 已加入任务计划 |

**红线**：外网访问**必须走 HTTPS**。用 HTTP 明文，账号密码会在网络上裸奔（隧道方案自带 HTTPS，端口映射方案必须自己配证书）。

---

## 1. 路线 A（推荐）：Cloudflare 隧道 —— 不需要公网 IP，不需要动路由器

**优点**：免费、自动 HTTPS、不暴露公司 IP、不用登录路由器；缺点是临时地址每次会变（要固定地址需自有域名）。

### A1. 临时地址（5 分钟，先试用）

```bat
python scripts\download_cloudflared.py      :: 下载隧道程序（已下过会自动跳过）
scripts\start_public_tunnel.bat             :: 一键启动，窗口里会打印外网地址
```

窗口里出现这样的地址就是外网入口（复制发给同事/客户即可）：

```
https://random-words-1234.trycloudflare.com
```

**查当前地址 / 生成二维码 / 自检**（推荐收藏这条命令）：

```bat
python scripts\tunnel_status.py
```

它会：列出正在运行的隧道 → 打印当前外网地址与厂内地址 → 逐个自检能不能通 →
把地址写到桌面 `QMS外网地址.txt` → 生成桌面 `QMS外网二维码.png`（手机扫码即进）。

**让它一直在线（推荐）**：跑一次 `scripts\install_autostart.bat`（用户级，不需要管理员），
之后每次开机登录会自动启动"系统 + 隧道"；隧道断了会自动重连，并把新地址更新到桌面。
> 想取消：删掉启动文件夹里的 `QMS系统.bat` / `QMS外网隧道.bat` 两个文件即可。

### A2. 固定域名（长期用，需要：一个域名 + 免费 Cloudflare 账号）

**已经为你准备好向导脚本**，你只需做两件人工的事，其余脚本自动完成：

1. **把域名接入 Cloudflare**（在 Cloudflare 网站添加站点，按提示把域名的 NS 记录改成 Cloudflare 给的）
2. **登录一次隧道账号**（在电脑上执行，会弹浏览器，点授权）：
   ```bat
   "%USERPROFILE%\cloudflared\cloudflared.exe" tunnel login
   ```
3. 然后一条命令搞定（自动创建隧道 → 写配置 → 绑定域名）：
   ```bat
   python scripts\cloudflare_setup.py qms.你的域名.com
   ```
4. 启动固定域名隧道：
   ```bat
   scripts\start_cloudflare_named.bat
   ```
   之后全员固定访问 `https://qms.你的域名.com`，HTTPS 自动续期，重启也不变。

随时可查状态：`python scripts\cloudflare_setup.py --check` 与 `python scripts\tunnel_status.py`。

---

## 2. 路线 B：公网 IP + 路由器端口映射（不依赖第三方，但需要动路由器）

本机当前公网 IP：**106.43.139.235**（贵州电信 AS4134；重启光猫可能变化，可申请固定 IP）。

1. **Windows 防火墙放行**（需管理员）：把 `scripts\open_firewall_public.bat` 右键"以管理员身份运行"
   （开放 18080 端口，不用 8000 避免与常见扫描撞车）
2. **路由器上做端口映射**：浏览器进路由器管理页（常见 192.168.1.1 / 192.168.100.1）→ 找"端口转发/虚拟服务器/NAT"→ 新增：
   - 外部端口：`18080`
   - 内部 IP：`192.168.100.197`（本机，建议在路由器里给它绑定固定 IP）
   - 内部端口：`8000`，协议 TCP
3. **确认运营商没封端口**：用手机流量（不连 WiFi）访问 `http://106.43.139.235:18080/api/health`，返回 `{"ok":true}` 即通。
   若不通：换成 443/8443 再试；电信家宽常封 80/443，高位端口通常可用。
4. **必须配 HTTPS**（否则密码明文外传）。最省事的做法：装 Caddy 反代并自动申请证书：
   - 需要域名解析到本机公网 IP，`caddy run` 用 443，反代到 `localhost:8000`，证书自动续期。
   - 没有域名就只能自签证书（浏览器会告警，不推荐给客户用）。

验证：`python scripts\check_public_access.py http://106.43.139.235:18080`

> 本机有多网卡：有线网卡空闲、只走 WiFi（192.168.100.197）。搬服务器后这条路线同样适用。

---

## 3. 路线 C：异地组网（Tailscale / 蒲公英）—— 只给内部人员用，最安全

装客户端（手机/电脑都装），登录同一账号后就像在同一个局域网里，用内网地址直连。
**优点**：完全不暴露到公网，不需要公网 IP、不动路由器；**缺点**：每台设备都要装客户端，客户扫码看 COA 用不了。

---

## 4. 让服务/隧道开机自启（长期运行必备）

| 项目 | 做法 |
|---|---|
| 系统本身 | `scripts\run_server.bat` 加入"任务计划程序"（登录时启动），见《运维手册》 |
| Cloudflare 隧道 | 把 `scripts\start_public_tunnel.bat` 也加入任务计划（触发条件=登录时） |
| 数据库/备份 | `scripts\daily_backup.bat` 每日任务 |

> 说明：Hermes 当前以普通权限运行，**无法**替你创建系统服务/改防火墙/改路由器；
> 需要管理员权限的两件事（防火墙放行、创建 Windows 服务）已做成 `scripts\open_firewall_public.bat`，
> 由你在本机右键"以管理员身份运行"。

---

## 5. 出问题怎么查（按顺序）

| 现象 | 先查什么 |
|---|---|
| 外网打不开 | ① 本机 `python main.py` 是否在跑（本机浏览器开 http://localhost:8000）② 隧道窗口是否还开着 ③ 局域网内另一台设备能否打开 http://192.168.100.197:8000 |
| 提示"账号已锁定" | 连错 5 次会锁 10 分钟（防爆破）。管理员解锁：**📜 操作日志 → 登录锁定 → 清空**（接口 `POST /api/admin/login-locks/clear`） |
| 客户说报告打不开 | COA 链接是**带口令**的，必须用完整链接（别只发编号）；口令被重置过就用新链接 |
| 地址每天变 | 路线 A1 是临时地址；改 A2 固定域名，或走路线 B |

---

## 6. ⚠️ 安全检查时发现的问题（请尽快处理）

1. **远程桌面（RDP 3389）被发布到了公网**：本机 cpolar 配置里有一条 `remoteDesktop` 隧道
   （`tcp://12.tcp.vip.cpolar.cn:10028`），日志显示 2026-09-11 有**境外 IP**（185.165.44.167、
   176.120.22.61、206.189.80.128）在连接它。RDP 暴露在公网极易被暴力破解/勒索。
   **建议**：不需要就关掉该隧道；需要远程桌面请改走路线 C（异地组网）或带双因素的方案。
2. **cpolar 免费版只能同时跑 1 条隧道**，目前被远程桌面占用 → 指向 8000 的两条隧道起不来。
   要么升级 cpolar，要么改用路线 A（Cloudflare，免费且不限条数）。
3. 演示账号 `123456` 未改前，**不要**对外暴露。

---

## 7. 最省事的推荐组合

- **长期用**：路线 A2（Cloudflare 固定域名）+ 任务计划自启 + 每日备份 + 全员改强密码
- **只是老板/业务员在外面看**：路线 C（异地组网），最安全，零暴露
- **想自己完全掌控、有固定公网 IP**：路线 B + Caddy 自动 HTTPS
