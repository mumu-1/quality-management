# 生产质量管理系统 · 运维手册（上线部署与维护）

> 面向：负责部署和维护这套系统的人（IT / 指定管理员）
> 配套脚本都在 `scripts/` 目录，命令中的路径按你的实际安装目录调整

---

## 一、系统概览

| 项目 | 说明 |
|---|---|
| 程序 | Python + FastAPI（Web 服务，浏览器访问，无需装客户端） |
| 数据库 | 开发用 SQLite 单文件；**上线用 MySQL 8**（通过环境变量 `QMS_DB_URL` 切换，业务代码不用改） |
| 界面 | 单页应用（电脑端三段式 + 深色车间大屏 + 客户扫码公开页） |
| 端口 | 默认 **8000**（服务监听 `0.0.0.0`，局域网可直接访问） |
| 账号规模 | 已验证 1500 账号批量创建；高峰并发目标 100~200 |

**目录结构**

```
quality-management\
├─ main.py              主程序（接口 + 页面服务）
├─ models.py            数据模型（表结构）
├─ database.py          数据库连接（QMS_DB_URL 切换 SQLite/MySQL）
├─ requirements.txt     依赖清单
├─ static\index.html    前端页面
├─ scripts\             运维与业务脚本（见下）
├─ backups\             备份目录（自动创建）
├─ qms.db               开发演示库（上线后由 MySQL 取代）
└─ *.md                 设计/计划/手册文档
```

**scripts 目录工具一览**

| 脚本 | 用途 |
|---|---|
| `init_db.py` | 建库 + 演示数据（首次部署或演练用；**生产环境勿跑**，会插演示数据） |
| `migrate_to_mysql.py` | 开发库 → MySQL 迁移（dryrun / run / verify） |
| `backup.py` | 备份 / 列表 / 校验 / 恢复（保留 30 天） |
| `batch_users.py` | 批量创建账号 + 导出初始密码表 |
| `load_test.py` | 并发压测 |
| `run_server.bat` | 生产模式启动（4 worker） |
| `daily_backup.bat` | 每日备份（配计划任务） |
| `e2e_step*.py` | 六套自动验收脚本（共 212 项检查） |

---

## 二、服务器部署（方案 C：先高配台式机过渡，随时平滑升级）

> 建议配置：i7 / 32G 内存 / 512G 固态，Windows 10/11 或 Linux 均可。
> 数据全部在 MySQL 里，日后换正式服务器只需把 MySQL 数据搬过去，业务不停。

### 第 1 步：装基础环境

1. **Python 3.12**（安装时勾选 "Add Python to PATH"）
2. **MySQL 8.0**（记住 root 密码；安装时字符集选 `utf8mb4`）
3. 安装依赖（在项目目录执行，国内建议加清华镜像）：

```bat
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 第 2 步：建 MySQL 数据库和账号

用 MySQL 客户端（或 Navicat）执行：

```sql
CREATE DATABASE qms DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'qms'@'%' IDENTIFIED BY '换成你的强密码';
GRANT ALL PRIVILEGES ON qms.* TO 'qms'@'%';
FLUSH PRIVILEGES;
```

### 第 3 步：把开发库数据搬过去（迁移）

```bat
REM 先干跑一次，生成 SQL 文件检查（不碰目标库）
python scripts\migrate_to_mysql.py dryrun --out migrate_qms.sql

REM 再真正迁移（自动建表 + 分批插数据 + 逐表校验）
set QMS_DB_URL=mysql+pymysql://qms:你的密码@127.0.0.1:3306/qms
python scripts\migrate_to_mysql.py run
```

脚本会打印每张表的「源 → 目标」行数对照；**全部一致才算成功**（出现 ✘ 时不要切换系统，先排查）。
也可以只用 `verify` 复查：`python scripts\migrate_to_mysql.py verify`

> 全新上线（没有历史数据）可跳过迁移，直接建空库后运行 `init_db.py` 的基础资料部分，或手工导入真实基础资料。

### 第 4 步：启动服务（生产模式）

```bat
set QMS_DB_URL=mysql+pymysql://qms:你的密码@127.0.0.1:3306/qms
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

或直接双击 `scripts\run_server.bat`（已内置 4 worker 与地址提示）。

启动后终端会显示：
- 本机访问：`http://localhost:8000`
- **局域网/手机访问**：`http://192.168.x.x:8000`（车间电脑、手机连同一个网络/WiFi 即可打开）

### 第 5 步：放行防火墙（局域网访问必需）

以管理员身份运行一次（只需一次）：

```bat
netsh advfirewall firewall add rule name="QMS-8000" dir=in action=allow protocol=TCP localport=8000
```

### 第 6 步：设置开机自启

- **简单做法（无需管理员）**：把 `scripts\run_server.bat` 的快捷方式放进启动文件夹
  `Win+R` → 输入 `shell:startup` → 把快捷方式拖进去
- 或注册计划任务（管理员）：
  ```bat
  schtasks /create /tn "QMS-Server" /tr "D:\qms\scripts\run_server.bat" /sc onstart /ru %USERNAME%
  ```

### 第 7 步：验证

1. 浏览器打开局域网地址，用管理员账号登录；
2. 手机连同一 WiFi 打开同一地址，能登录即通过（**内网/手机访问验收**）；
3. 跑一次压测：
   ```bat
   python scripts\load_test.py --users 200 --seconds 20
   ```
   关注三项：成功率 ≥ 99%、P95 < 2000ms、平均 < 1000ms。

---

## 三、日常运维

### 3.1 备份（每天一次，保留 30 天）

```bat
python scripts\backup.py backup     REM 备份
python scripts\backup.py list       REM 查看已有备份
python scripts\backup.py verify <备份文件>   REM 校验备份可读
```

- 开发库（SQLite）：用在线备份接口，**服务运行中也能备份**，不锁库；
- 生产库（MySQL）：自动改用 `mysqldump`（需本机有 mysqldump，一般随 MySQL 安装）；
- 超 30 天的备份自动清理。

**配置每日自动备份**（计划任务，每天 23:30）：
```bat
schtasks /create /tn "QMS-DailyBackup" /tr "D:\qms\scripts\daily_backup.bat" /sc daily /st 23:30 /ru %USERNAME%
```
备份日志写在 `backups\backup.log`。

### 3.2 恢复（演练过的步骤）

```bat
REM 1) 停服务（关闭运行窗口即可）
REM 2) 恢复（脚本会先把当前库另存一份，防止误操作）
python scripts\backup.py restore backups\qms_20260910_233000.db
REM 3) 重新启动服务，登录抽查数据
```

> MySQL 环境恢复：`mysql -uqms -p qms < backups\qms_mysql_20260910_233000.sql`

**要求**：上线后至少**真演练一次**（备份 → 随便改个数据 → 恢复 → 数据回到备份时点）。

### 3.3 批量建账号

准备名单 CSV（表头：`姓名,部门,岗位,工序编码,用户名`；工序编码可留空，角色按岗位自动映射）：

```bat
python scripts\batch_users.py csv 名单.csv
python scripts\batch_users.py demo 1500        REM 演示：按部门/角色分布生成 1500 个
python scripts\batch_users.py csv 名单.csv --password 123456   REM 统一初始密码
```

- 生成后导出 **`账号初始密码表_时间戳.csv`**（含明文初始密码）：打印裁剪后发给个人，**发完删除该文件**；
- 已存在的用户名自动跳过（可反复运行补录）；
- 岗位→角色映射：检验员/质检员→检验员，班组长/班长→班组长，仓管→仓储，采购→采购，操作工→操作工，质量经理→质量经理，取样员→取样员，高层→高层，系统管理员→管理员；
- 角色只是**默认模板**：每个账号可在「账号管理 → 编辑」里单独勾选可见页面、可管模块、负责工序（支持一人多工序）。

### 3.4 基础资料导入

「基础资料」各页面（物料/供应商/客户/车间/工序/班组/设备）都有 **下载导入模板** 和 **导入** 按钮。
按模板整理 Excel → 另存为 CSV（UTF-8）→ 导入 → 页面会给出「成功 N 条 / 失败 M 条 + 失败原因」。

### 3.5 账号与权限维护

- 忘记密码：管理员在「账号管理」点「重置密码」；
- 人员调岗：编辑账号 → 改角色（套用新默认模板）→ 再按需微调勾选项；
- 离职：停用账号（立即无法登录，历史记录保留）。

---

## 四、常见问题排查

| 现象 | 处理 |
|---|---|
| 打不开页面 | 确认服务窗口还在运行；确认地址端口 8000；局域网访问需放行防火墙 |
| 手机打不开 | 手机与服务器要在**同一个网络/WiFi**；用服务器局域网 IP 而不是 localhost |
| 启动报端口占用 | 改端口：`--port 8001`；或先关掉占用 8000 的旧窗口 |
| `database is locked` | SQLite 并发写限制；生产务必切 MySQL（见第二节） |
| 迁移后行数不一致 | 不要切换系统，先看 `verify` 输出定位是哪张表；常见是源库正在被写（先停服务再迁） |
| 备份文件很小（几 KB） | 说明当时数据库是空的；检查 `QMS_DB_URL` 是否指向了正确的库 |
| 忘记管理员密码 | 停止服务，用 Python 执行 `hash_pwd` 重置（见下） |

重置管理员密码小技巧（停服务后执行）：

```python
python -c "import sys;sys.path.insert(0,'.');from database import SessionLocal;import models as M;from main import hash_pwd;db=SessionLocal();u=db.query(M.User).filter(M.User.username=='admin').first();u.password_salt,u.password_hash=hash_pwd('新密码');db.commit();print('已重置')"
```

---

## 六、压测实测记录（首次上线参考）

**测试环境（本机开发机，非服务器）**：i5-6300HQ（4 核）/ 32G 内存 / Windows 10 / SQLite 开发库。
**注意**：压测客户端与服务端跑在**同一台机器**上，两者抢 CPU，结果偏悲观——真实服务器上从另一台电脑发起压测会明显更好。

| 场景 | 并发 | 请求数 | 成功率 | QPS | 平均 | P95 |
|---|---|---|---|---|---|---|
| 混合读写（单进程） | 200 | 905 | **100%** | 33.9 | 4867 ms | 8516 ms |
| 纯读（单进程） | 200 | 802 | **100%** | 33.9 | 4867 ms | 8516 ms |
| 纯读（4 worker） | 200 | 1093 | **100%** | **45.7** | **3434 ms** | 7328 ms |

**结论与建议**
1. **稳定性好**：任何场景下成功率都是 100%，没有一次失败/超时（说明不是系统缺陷）。
2. 多 worker 明显有效：QPS +35%、平均响应 -29%（正式部署务必用 `--workers 4`，脚本已内置）。
3. 本机不达标的原因是**单机自压测把 4 个 CPU 核跑满**（服务端 4 进程 + 客户端 200 线程）。
4. 上线后请在**服务器上、从另一台电脑**执行 `python scripts\load_test.py --users 200 --seconds 20`，
   达标线：成功率 ≥ 99%、P95 < 2000 ms、平均 < 1000 ms。
5. 若服务器上仍不达标，按顺序检查：① 是否用了 MySQL（不是 SQLite）② 是否 `--workers 4+` ③ 慢查询（给 `test_record/trace` 加索引）④ 服务器是否被其他程序占资源。

---

## 七、上线检查清单（逐项打勾）

- [ ] MySQL 已安装，`qms` 库与账号已建
- [ ] 迁移完成，`verify` 显示全部表行数一致
- [ ] 服务以生产模式启动（4 worker），`QMS_DB_URL` 指向 MySQL
- [ ] 防火墙已放行 8000；**车间电脑**与**手机**都能打开登录页
- [ ] 开机自启已配置，重启服务器后服务自动起来
- [ ] 每日备份计划任务已配置，并**真演练过一次恢复**
- [ ] 压测通过（200 并发，成功率 ≥ 99%，P95 < 2 秒）
- [ ] 真实基础资料已导入（物料/供应商/客户/车间/工序/班组/设备）
- [ ] 1500 账号已批量创建，初始密码表已发放并删除
- [ ] 各角色培训完成（见《各角色操作手册》），关键岗位会用手机端
- [ ] 管理员已改掉演示密码，并停用不需要的演示账号
